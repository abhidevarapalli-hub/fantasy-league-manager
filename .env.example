# Production Supabase credentials
# Copy this to .env and fill in real values for production builds
VITE_SUPABASE_PROJECT_ID="your-supabase-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-supabase-anon-key"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"

# RapidAPI Cricbuzz Cricket API
VITE_RAPIDAPI_KEY=your-rapidapi-key
VITE_RAPIDAPI_HOST=cricbuzz-cricket.p.rapidapi.com

# NOTE: For local development, `npm run dev` automatically uses the local
# Supabase instance via .env.development. You only need .env for production
# builds or to override local defaults via .env.development.local (git-ignored).
